{
  "name": "GSUPizzaShop",
  "scripts": {
    "pg":"backups capture --app PRODUCTION"
    "pg":"backups restore heroku pg:backups public-url --app PRODUCTION DATABASE_URL --app REVIEW"
  },
  "env": {
    "DATABASE_URL": {
      "required": true
    },
    "HEROKU_APP_NAME": {
      "required": true
    }
  },
  "formation": {
  },
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "heroku/php"
    }
  ]
}
